---
title: Types
description: CellStyle, SummaryDefinition, SchemaDefinition, WorkbookStreamOptions, SheetOptions, and TableInput shapes.
---

## `CellStyle`

Applied to data cells via `style`, to header cells via `headerStyle`, and to hyperlink-local style overrides.

```ts twoslash
import type { CellStyle } from "@chronicstone/typed-xlsx";
```

| Field        | Type                  | Description                                      |
| ------------ | --------------------- | ------------------------------------------------ |
| `font`       | `FontStyle`           | Font properties.                                 |
| `fill`       | `FillStyle`           | Background fill.                                 |
| `border`     | `BorderStyle`         | Cell border lines.                               |
| `alignment`  | `AlignmentStyle`      | Text alignment and wrap.                         |
| `numFmt`     | `string`              | Excel number format string.                      |
| `protection` | `CellProtectionStyle` | Worksheet protection flags (`locked`, `hidden`). |

`style` is resolved by the library during export.

`conditionalStyle` does not use the full `CellStyle` surface: worksheet protection flags are intentionally excluded because native Excel conditional formatting cannot toggle `locked` or `hidden`.

---

## `ConditionalStyleInput<TColumnId, TGroupId>`

Accepted by the `conditionalStyle` field on column definitions.

```ts twoslash
import type { ConditionalStyleInput } from "@chronicstone/typed-xlsx";
```

It can be:

- a callback builder `(conditional) => conditional.when(...)`
- an array of normalized conditional style rules

The callback form is the recommended public API.

### `ConditionalStyleBuilder<TColumnId, TGroupId>`

```ts twoslash
import type { ConditionalStyleBuilder } from "@chronicstone/typed-xlsx";
```

| Method                   | Description                                                                                                                   |
| ------------------------ | ----------------------------------------------------------------------------------------------------------------------------- |
| `when(condition, style)` | Adds one conditional-formatting rule using the formula DSL for `condition` and a visual style overlay for the matching cells. |
| `done()`                 | Returns the normalized rule array. Mostly useful internally or for advanced composition.                                      |

Example:

```ts twoslash
import { createExcelSchema } from "@chronicstone/typed-xlsx";

const schema = createExcelSchema<{ amount: number; quota: number }>()
  .column("amount", { accessor: "amount" })
  .column("quota", { accessor: "quota" })
  .column("attainment", {
    formula: ({ row }) => row.ref("amount").div(row.ref("quota")),
    conditionalStyle: (conditional) =>
      conditional.when(({ row }) => row.ref("attainment").lt(1), {
        fill: { color: { rgb: "FEE2E2" } },
        font: { color: { rgb: "991B1B" }, bold: true },
      }),
  })
  .build();
```

### `FontStyle`

| Field       | Type              | Description                                  |
| ----------- | ----------------- | -------------------------------------------- |
| `name`      | `string`          | Font family name (e.g. `"Calibri"`).         |
| `size`      | `number`          | Font size in points.                         |
| `bold`      | `boolean`         | Bold text.                                   |
| `italic`    | `boolean`         | Italic text.                                 |
| `underline` | `boolean`         | Underline text.                              |
| `strike`    | `boolean`         | Strikethrough text.                          |
| `color`     | `{ rgb: string }` | Font color as a 6-digit hex string (no `#`). |

### `FillStyle`

| Field   | Type              | Description                                        |
| ------- | ----------------- | -------------------------------------------------- |
| `color` | `{ rgb: string }` | Background color as a 6-digit hex string (no `#`). |

### `BorderStyle`

| Field    | Type         | Description    |
| -------- | ------------ | -------------- |
| `top`    | `BorderEdge` | Top border.    |
| `right`  | `BorderEdge` | Right border.  |
| `bottom` | `BorderEdge` | Bottom border. |
| `left`   | `BorderEdge` | Left border.   |

### `BorderEdge`

| Field   | Type              | Description                           |
| ------- | ----------------- | ------------------------------------- |
| `style` | `BorderEdgeStyle` | Line style (see below).               |
| `color` | `{ rgb: string }` | Border color as a 6-digit hex string. |

`BorderEdgeStyle` values: `"thin"` · `"medium"` · `"thick"` · `"dashed"` · `"dotted"` · `"double"` · `"hair"` · `"dashDot"` · `"dashDotDot"`

### `AlignmentStyle`

| Field        | Type                                                       | Description                |
| ------------ | ---------------------------------------------------------- | -------------------------- |
| `horizontal` | `"left"` · `"center"` · `"right"` · `"fill"` · `"justify"` | Horizontal alignment.      |
| `vertical`   | `"top"` · `"center"` · `"bottom"` · `"justify"`            | Vertical alignment.        |
| `wrapText`   | `boolean`                                                  | Wrap text within the cell. |

### `CellProtectionStyle`

| Field    | Type      | Description                                                       |
| -------- | --------- | ----------------------------------------------------------------- |
| `locked` | `boolean` | Whether the cell should be locked when the sheet is protected.    |
| `hidden` | `boolean` | Whether the formula should be hidden when the sheet is protected. |

---

## `HyperlinkInput<T>`

Passed to the `hyperlink` field on column definitions.

```ts twoslash
import type { CellStyle } from "@chronicstone/typed-xlsx";
```

It can be:

- a `string` target
- an object `{ target, tooltip?, style? }`
- a callback returning one of those or `null` / `undefined`

`style` only applies when the hyperlink exists.

---

## `WorkbookProtectionOptions`

Used by `createWorkbook({ protection })` and `createWorkbookStream({ protection })`.

| Field       | Type      | Description                                             |
| ----------- | --------- | ------------------------------------------------------- |
| `password`  | `string`  | Optional Excel workbook-protection password.            |
| `structure` | `boolean` | Lock workbook structure (sheet add/remove/rename/move). |
| `windows`   | `boolean` | Lock workbook window metadata.                          |

---

## `SheetProtectionOptions`

Used by `.sheet(name, { protection })` on both builders.

| Field                 | Type      | Description                                       |
| --------------------- | --------- | ------------------------------------------------- |
| `password`            | `string`  | Optional Excel worksheet-protection password.     |
| `selectLockedCells`   | `boolean` | Allow selecting locked cells while protected.     |
| `selectUnlockedCells` | `boolean` | Allow selecting unlocked cells while protected.   |
| `formatCells`         | `boolean` | Allow formatting cells while protected.           |
| `formatColumns`       | `boolean` | Allow formatting columns while protected.         |
| `formatRows`          | `boolean` | Allow formatting rows while protected.            |
| `insertColumns`       | `boolean` | Allow inserting columns while protected.          |
| `insertRows`          | `boolean` | Allow inserting rows while protected.             |
| `insertHyperlinks`    | `boolean` | Allow inserting hyperlinks while protected.       |
| `deleteColumns`       | `boolean` | Allow deleting columns while protected.           |
| `deleteRows`          | `boolean` | Allow deleting rows while protected.              |
| `sort`                | `boolean` | Allow using sort while protected.                 |
| `autoFilter`          | `boolean` | Allow using existing autofilters while protected. |
| `pivotTables`         | `boolean` | Allow using pivot tables while protected.         |
| `objects`             | `boolean` | Allow editing objects while protected.            |
| `scenarios`           | `boolean` | Allow editing scenarios while protected.          |

---

## `SummaryInput<T>`

Passed to the `summary` field of a column definition.

```ts twoslash
import type { SummaryBuilder, SummaryDefinition, SummaryInput } from "@chronicstone/typed-xlsx";
```

`SummaryInput<T>` can be:

- a single `SummaryDefinition<T, TAcc>`
- an array of `SummaryDefinition<T, TAcc>`
- a callback `(summary: SummaryBuilder<T>) => SummaryDefinition[]`

The callback form is the recommended public API.

---

## `LazyText`

Used for authored display strings that should be resolved lazily during export.

```ts twoslash
import type { LazyText } from "@chronicstone/typed-xlsx";
```

```ts
type LazyText = string | (() => string);
```

Supported on user-facing authored labels such as:

- column `header`
- summary `label(...)`
- Excel-table `totalsRow.label`
- validation `prompt.title`, `prompt.message`, `error.title`, and `error.message`

---

## `ValidationInput<TColumnId, TGroupId>`

Passed to the `validation` field on column definitions.

```ts twoslash
import type { ValidationBuilder, ValidationInput, ValidationRule } from "@chronicstone/typed-xlsx";
```

It can be:

- a callback builder `(validation) => validation.integer().between(...)`
- a normalized `ValidationRule<TColumnId, TGroupId>` object

The callback form is the recommended public API.

### `ValidationBuilder<TColumnId, TGroupId>`

| Method                        | Description                                                                |
| ----------------------------- | -------------------------------------------------------------------------- |
| `list(values)`                | Restrict values to a dropdown list.                                        |
| `integer()`                   | Restrict values to integers. Compiles to Excel's `whole` validation kind.  |
| `decimal()`                   | Restrict values to decimal numbers.                                        |
| `date()`                      | Restrict values to dates.                                                  |
| `textLength()`                | Restrict values by string length.                                          |
| `custom(condition)`           | Use the formula DSL for a custom validation condition.                     |
| `between(min, max)`           | Set an inclusive range operator.                                           |
| `notBetween(min, max)`        | Set an exclusive range operator.                                           |
| `eq(value)`                   | Set the `equal` operator.                                                  |
| `neq(value)`                  | Set the `notEqual` operator.                                               |
| `gt(value)`                   | Set the `greaterThan` operator.                                            |
| `gte(value)`                  | Set the `greaterThanOrEqual` operator.                                     |
| `lt(value)`                   | Set the `lessThan` operator.                                               |
| `lte(value)`                  | Set the `lessThanOrEqual` operator.                                        |
| `allowBlank(value?)`          | Allow blanks in the validated range.                                       |
| `showDropDown(value?)`        | Control Excel's dropdown arrow behavior for list validations.              |
| `prompt({ title?, message })` | Configure the input message shown when the cell is selected.               |
| `error({ title?, message })`  | Configure the blocking error alert shown for invalid edits.                |
| `done()`                      | Return the normalized rule object. Mostly useful for advanced composition. |

### `ValidationRule<TColumnId, TGroupId>`

Normalized validation shape used internally and exposed for advanced composition.

| Field          | Type                                                                   | Description                                         |
| -------------- | ---------------------------------------------------------------------- | --------------------------------------------------- |
| `type`         | `"list" \| "whole" \| "decimal" \| "date" \| "textLength" \| "custom"` | Excel validation kind.                              |
| `operator`     | comparison operator                                                    | Optional range/comparison operator.                 |
| `formula1`     | scalar value or formula expr                                           | First comparison input.                             |
| `formula2`     | scalar value or formula expr                                           | Second comparison input for two-value operators.    |
| `source`       | `Array<string \| number>`                                              | Inline source for `list(...)`.                      |
| `allowBlank`   | `boolean`                                                              | Whether blanks should pass validation.              |
| `showDropDown` | `boolean`                                                              | Excel dropdown-arrow behavior for list validations. |
| `prompt`       | `string \| ValidationMessage`                                          | Input message metadata.                             |
| `error`        | `string \| ValidationMessage`                                          | Error alert metadata.                               |

### `ValidationMessage`

```ts twoslash
import type { ValidationMessage } from "@chronicstone/typed-xlsx";
```

| Field     | Type       | Description                     |
| --------- | ---------- | ------------------------------- |
| `title`   | `LazyText` | Optional prompt or error title. |
| `message` | `LazyText` | Prompt or error body text.      |

---

## `SummaryDefinition<T, TAcc>`

```ts twoslash
import type { SummaryDefinition } from "@chronicstone/typed-xlsx";
```

| Field      | Type                                                    | Description                                                          |
| ---------- | ------------------------------------------------------- | -------------------------------------------------------------------- |
| `label`    | `string \| (() => string)`                              | Optional label rendered in non-summary columns on the same row.      |
| `init`     | `() => TAcc`                                            | Returns the initial accumulator. Called once before the first batch. |
| `step`     | `(acc: TAcc, row: T, rowIndex: number) => TAcc`         | Called for each row in each committed batch.                         |
| `finalize` | `(acc: TAcc) => SummaryCellValue`                       | Called once at finalization. Returns the cell value.                 |
| `format`   | `string` or `(value: SummaryCellValue) => string`       | Excel number format applied to the summary cell.                     |
| `style`    | `CellStyle` or `(value: SummaryCellValue) => CellStyle` | Style applied to the summary cell.                                   |

---

## `SummaryBuilder<T>`

Helper object passed into the callback form of `summary`.

| Method           | Description                                                    |
| ---------------- | -------------------------------------------------------------- |
| `cell({...})`    | Creates a reducer-based summary cell.                          |
| `formula(...)`   | Creates a summary cell backed by an Excel range formula.       |
| `label("Total")` | Creates a static or lazy string summary cell.                  |
| `empty()`        | Creates an intentionally blank summary cell for row alignment. |
| `spacer()`       | Creates an empty summary cell without default summary styling. |

---

## `SchemaDefinition<T, TColumnId = string, TGroupId = never, TGroupContext = {}>`

The return type of `createExcelSchema<T>().build()`. Opaque — do not construct manually.

Schemas are frozen after `.build()` and safe to share across multiple workbooks and threads.

`TColumnId` carries the compile-time union of known column ids. `TGroupId` carries the compile-time union of group ids. Both are used to type `select.include` and `select.exclude` on the buffered and stream builders.

`TGroupContext` carries the required shape of the `context` object for groups whose callback declares a second parameter. If a schema defines `.group("orgs", (builder, ctx: Org[]) => ...)`, table inputs must provide `context.orgs` when that group is selected.

---

## `CellValue`

The set of primitive values that can appear in a cell.

```ts
type CellValue = string | number | boolean | Date | null | undefined;
```

Array values (`string[]`, `number[]`) are accepted by `accessor` and `transform` to trigger sub-row expansion. The array element type must be `CellValue`.

---

## `WorkbookStreamOptions`

Options for `createWorkbookStream()`.

| Field           | Type                                             | Default      | Description                                                   |
| --------------- | ------------------------------------------------ | ------------ | ------------------------------------------------------------- |
| `tempStorage`   | `"file"` · `"memory"`                            | `"file"`     | Where serialized sheet fragments are spooled between commits. |
| `tempDirectory` | `string`                                         | OS temp dir  | Directory for file-backed spools.                             |
| `strings`       | `"auto"` · `"inline"` · `"shared"`               | `"auto"`     | String storage strategy. Overrides `memoryProfile`.           |
| `memoryProfile` | `"balanced"` · `"low-memory"` · `"compact-file"` | `"balanced"` | Convenience preset for string strategy.                       |

String strategy meanings:

| Value      | Description                                                                                                                 |
| ---------- | --------------------------------------------------------------------------------------------------------------------------- |
| `"inline"` | Each string written directly into the cell XML. No shared string table in heap. Produces larger files.                      |
| `"shared"` | All strings deduplicated into a shared string table. Smaller output files for repeated values, but the table grows in heap. |
| `"auto"`   | Uses `"balanced"` preset: shared strings enabled, balanced memory/size trade-off.                                           |

`memoryProfile` presets:

| Preset           | Equivalent strings setting | Use when                                     |
| ---------------- | -------------------------- | -------------------------------------------- |
| `"balanced"`     | `"shared"`                 | Default. General purpose.                    |
| `"low-memory"`   | `"inline"`                 | Minimizing heap is the priority.             |
| `"compact-file"` | `"shared"`                 | Minimizing output file size is the priority. |

---

## `SheetOptions`

Options for `.sheet(name, options?)` on both builders.

| Field            | Type                                  | Description                                                         |
| ---------------- | ------------------------------------- | ------------------------------------------------------------------- |
| `tablesPerRow`   | `number`                              | Number of tables placed side-by-side before wrapping. Default: `1`. |
| `tableColumnGap` | `number`                              | Blank columns between tables in the same row. Default: `1`.         |
| `tableRowGap`    | `number`                              | Blank rows between table rows. Default: `1`.                        |
| `freezePane`     | `{ rows?: number; columns?: number }` | Freeze the first N rows and/or columns.                             |
| `rightToLeft`    | `boolean`                             | Enable right-to-left sheet direction.                               |

`WorkbookStream.sheet()` accepts the same options.

---

## `TableInput<T>`

Passed as the second argument to `.table(id, input)` on the buffered builder.

| Field     | Type                                                                                                                                                     | Description                                                     |
| --------- | -------------------------------------------------------------------------------------------------------------------------------------------------------- | --------------------------------------------------------------- |
| `schema`  | `SchemaDefinition<T>`                                                                                                                                    | Built schema.                                                   |
| `rows`    | `T[]`                                                                                                                                                    | Data rows.                                                      |
| `title`   | `string`                                                                                                                                                 | Optional title row rendered above the header.                   |
| `select`  | `{ include?: readonly (SchemaColumnId<TSchema> \| SchemaGroupId<TSchema>)[]; exclude?: readonly (SchemaColumnId<TSchema> \| SchemaGroupId<TSchema>)[] }` | Column or group selection by id.                                |
| `context` | `SchemaGroupContext<TSchema>`                                                                                                                            | Required only when selected groups declare a context parameter. |

---

## `StreamTableInput<T>`

Passed as the second argument to the async `.table(id, input)` on the stream builder.

| Field     | Type                                                                                                                                                     | Description                                                     |
| --------- | -------------------------------------------------------------------------------------------------------------------------------------------------------- | --------------------------------------------------------------- |
| `schema`  | `SchemaDefinition<T>`                                                                                                                                    | Built schema.                                                   |
| `select`  | `{ include?: readonly (SchemaColumnId<TSchema> \| SchemaGroupId<TSchema>)[]; exclude?: readonly (SchemaColumnId<TSchema> \| SchemaGroupId<TSchema>)[] }` | Column or group selection by id.                                |
| `context` | `SchemaGroupContext<TSchema>`                                                                                                                            | Required only when selected groups declare a context parameter. |

`rows` is not present — rows are committed incrementally via `.commit({ rows })`.
