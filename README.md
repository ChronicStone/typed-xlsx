# typed-xlsx workspace

This repository now uses a workspace layout:

- `packages/typed-xlsx`: the publishable library package
- `docs`: the Docus-powered documentation app

Local commands:

```bash
bun install
bun run build
bun run docs:dev
```

The package README lives in `packages/typed-xlsx/README.md`.
