{
  "name": "typed-xlsx-workspace",
  "version": "1.0.0",
  "private": true,
  "description": "Workspace root for typed-xlsx package and documentation",
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "type": "module",
  "scripts": {
    "format": "vp fmt",
    "format:check": "vp fmt --check",
    "lint": "vp lint",
    "typecheck": "turbo run typecheck",
    "test": "turbo run test",
    "build": "turbo run build",
    "pack:check": "turbo run pack:check",
    "check": "turbo run check",
    "examples:build": "turbo run build --filter=@chronicstone/typed-xlsx-examples",
    "benchmark:build": "turbo run build --filter=@chronicstone/typed-xlsx-benchmark",
    "docs:dev": "turbo run dev --filter=@chronicstone/typed-xlsx-docs",
    "docs:build": "turbo run build --filter=@chronicstone/typed-xlsx-docs",
    "docs:generate": "turbo run generate --filter=@chronicstone/typed-xlsx-docs",
    "docs:twoslash": "turbo run twoslash:verify --filter=@chronicstone/typed-xlsx-docs",
    "docs:check": "turbo run check --filter=@chronicstone/typed-xlsx-docs && turbo run twoslash:verify --filter=@chronicstone/typed-xlsx-docs",
    "ci": "turbo run check --filter=!@chronicstone/typed-xlsx-benchmark && turbo run twoslash:verify --filter=@chronicstone/typed-xlsx-docs",
    "prepare": "vp config",
    "changeset": "changeset",
    "release:status": "changeset status --verbose",
    "release:version": "changeset version && bun install",
    "release:publish": "changeset publish"
  },
  "devDependencies": {
    "@changesets/changelog-github": "0.6.0",
    "@changesets/cli": "2.30.0",
    "turbo": "2.5.8",
    "typescript": "6.0.2",
    "vite-plus": "0.1.15"
  },
  "packageManager": "bun@1.3.6"
}
